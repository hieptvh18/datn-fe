import classNames from 'classnames'
import React from 'react'
import styles from './styles.scss'
const cx = classNames.bind(styles)
const FooterIndex = () => {
    return (
        <div className={cx('title')} >FooterIndex</div>
    )
}

export default FooterIndex